# 开发板环境配置
## 1. 雷达环境配置
### 1.1 ROS安装
#### 1.1.1 前往[ROS官网](https://wiki.ros.org/cn/ROS/Installation)查看

## 2. 虚拟环境配置
### 2.1 conda create -n cosense python=3.8.20

### 2.2 pip install numpy

### 2.3 pip install -r requirments.txt

### 2.4 pip install rospy -i https://rospypi.github.io/simple/

### 2.5 pip install sensor_msgs geometry_msgs nav_msgs actionlib_msgs -i https://rospypi.github.io/simple/

### 2.6 安装pytorch for jetson
#### 2.6.1    查看jetpack版本  cat /etc/nv_tegra_release			# R35
#### 2.6.2    前往   https://forums.developer.nvidia.com/t/pytorch-for-jetson/72048  下载jetpack版本对应的torch wheel		# torch-1.13.0a0+d0d6b1f2.nv22.10-cp38-cp38-linux_aarch64.whl
#### 2.6.3    pip install torch-1.13.0a0+d0d6b1f2.nv22.10-cp38-cp38-linux_aarch64.whl

### 2.7 安装torchvision
#### 2.7.1  安装依赖  
1)  pip install numpy requests Pillow
2)  sudo apt install libjpeg-dev libpng-dev zlib1g-dev libpython3-dev libavcodec-dev libavformat-dev libswscale-dev python3-pip libopenblas-base libopenmpi-dev
#### 2.7.2  前往    https://github.com/pytorch/vision   
1)  查看torch与torchvision版本对应关系
2)  下载对应torchvision版本源码  git clone --branch v0.14 https://github.com/pytorch/vision
#### 2.7.3  编译安装  
1)  cd vision		                   		# 进入torchvision目录
2)  export BUILD_VERSION=0.14.0      	# 将BUILD_VERSION环境变量设置为值 0.14.0  
3)  python3 setup.py install --user     	# 使用 Python 的 setuptools 工具将vision包安装到当前用户的本地目录中
 
### 2.8 安装cumm
#### 2.8.1  前往  https://github.com/FindDefinition/cumm  下载cumm0.2.9版本源码  git clone -b v0.2.9 https://github.com/FindDefinition/cumm
#### 2.8.2  编译安装  
1)  cd cumm
2)  export CUMM_CUDA_ARCH_LIST="8.7"		# xavier是7.2，TX2是6.2，orin是8.7
3)  export CUMM_CUDA_VERSION="11.4"		# 11.4为cuda版本
4)  export CUMM_DISABLE_JIT="1"			# 不用JIT编译cumm，而是编译成whl后再安装
5)  python setup.py bdist_wheel				# 编译生成cumm的whl在dist文件夹内
6)  pip install dists/xxx.whl # 安装编译好的cumm的whl，名字应该类似cumm_cu114-0.2.9-cp38-cp38m-linux_aarch64.whl

### 2.9 安装spconv
#### 2.9.1  前往  https://github.com/traveller59/spconv  下载spconv2.1.25版本源码  git clone -b v2.1.25 https://github.com/traveller59/spconv --recursive
#### 2.9.2  前往spconv目录，删除pyproject.toml文件其中关于cumm的版本要求
#### 2.9.3  编译安装  
1)  cd spconv
2)  export CUMM_CUDA_ARCH_LIST="8.7"		# xavier是7.2，TX2是6.2，orin是8.7
3)  export CUMM_CUDA_VERSION="11.4"		# 11.4为cuda版本
4)  export SPCONV_DISABLE_JIT="1"			# 不用JIT编译spconv，而是编译成whl后再安装
5)  python setup.py bdist_wheel				# 编译生成spconv的whl在dist文件夹内
6)  pip install dists/xxx.whl # 安装编译好的cumm的whl，名字应该类似spconv_cu114-2.1.25-cp38-cp38m-linux_aarch64.whl
